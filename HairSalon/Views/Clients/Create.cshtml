@{
  Layout = "_Base";
}
@using System.Collections.Generic;

@model HairSalon.Models.Client

<h4>Add a new client</h4>

@using (Html.BeginForm())
{
  @await Html.PartialAsync("Forms/ClientForm")
  <button>Add new client</button>
}
